// One record per live Azure Marketplace app, rendered by layouts/AzureAppPage.astro
// via the thin pages in src/pages/azure/. Screenshots live at public/azure/<slug>/s{1,2,3}.png
// (the same 1280x720 images submitted to Partner Center). Roles and Graph permissions are
// copied from each app's scripts/grant-*.ps1, which is what the customer actually runs.
// Copy rule: no em or en dashes in any user-facing string.
// price: flat USD per month as set on the Marketplace plan, or null when not yet confirmed here.

export interface AzureAppDetail {
  slug: string;
  name: string;
  group: string;
  title: string;         // SEO title / H1, search-intent phrased
  tagline: string;
  description: string;   // meta description
  problem: string;
  features: string[];
  captions: [string, string, string];
  honest: string[];
  roles: { role: string; why: string }[];
  writes: string | null; // the opt-in write path, or null if read-only
  alerts: string;        // where notifications go
  price: number | null;
  blog: string;
}

export const azureAppDetails: AzureAppDetail[] = [
  {
    slug: 'secret-sentinel', name: 'Secret Sentinel', group: 'Secrets & certificates',
    title: 'Get alerted before Entra app registration secrets and certificates expire',
    tagline: 'Every app registration in your tenant, every client secret and certificate, one Teams alert before any of them expires. Runs in your own tenant.',
    description: 'Entra ID shows the expiry date of every app registration secret and certificate but never alerts anyone. Secret Sentinel scans them all on a schedule and posts to Teams before they expire, from a managed application inside your tenant.',
    problem: 'An expired client secret takes an integration down silently, and the first sign is usually a failing pipeline or a support ticket. Entra shows every expiry date on the app registration blade and does nothing with it. The feedback request for native alerting has been open for years with hundreds of votes.',
    features: [
      'Enumerates every app registration in the tenant and every client secret and certificate on it.',
      'Alerts at your chosen thresholds before expiry, and keeps alerting daily until the credential is rotated.',
      'Reports expired credentials separately, so what is already broken is never buried under what is about to be.',
      'Reads credential metadata only: names, ids, and expiry dates. Secret values are never readable.',
      'Posts an Adaptive Card to your Teams channel; nothing is stored or sent anywhere else.',
    ],
    captions: ['The digest: every secret and certificate approaching expiry, grouped by app registration.', 'Overview of the tenant: how many credentials, how many expiring, how many already expired.', 'Function traces in your own Application Insights, so you can see each scan run.'],
    honest: ['It reports; it does not rotate. Rotation stays a deliberate human step with your deployment pipeline.', 'Credentials on enterprise applications (service principals) that are not app registrations in this tenant are outside its view.'],
    roles: [{ role: 'Microsoft Graph Application.Read.All', why: 'read app registrations and their credential metadata' }],
    writes: null, alerts: 'the Teams webhook you configure', price: null,
    blog: '/blog/entra-app-registration-secret-expiry-alerts',
  },
  {
    slug: 'cert-sentinel', name: 'Cert Sentinel', group: 'Secrets & certificates',
    title: 'Get alerted before TLS certificates expire on Azure App Gateway and App Service',
    tagline: 'A daily digest of every TLS certificate that is expired, expiring, or unreadable, across the stores Azure leaves without native alerting.',
    description: 'Azure has no near-expiry alerting for TLS certificates on Application Gateway listeners, App Service, or API Management. Cert Sentinel sweeps them daily from inside your subscription and posts one digest to Teams.',
    problem: 'An expired TLS certificate takes production down hard, and the places certificates actually live in Azure, Application Gateway listeners and trusted roots, App Service uploads, API Management, have no native expiry alert. Teams find out from customers.',
    features: [
      'Covers Application Gateway frontend and backend certificates, App Service certificates, and API Management certificates.',
      'Reports expired, expiring within your thresholds (default 30, 14, 7, and 1 days), and could-not-read certificates, which is itself a signal.',
      'Re-reports every day until a certificate is renewed, so an ignored warning escalates rather than disappears.',
      'Decodes Application Gateway certificate bundles correctly (they are PKCS7, not bare X.509) and reads the leaf expiry.',
      'Reads only certificate metadata and public certificate data through the management plane. Private keys are never touched.',
    ],
    captions: ['The daily digest: expired, expiring, and unreadable certificates across every covered store.', 'Per-certificate detail with the resource, listener, and days remaining.', 'Thresholds and the Teams destination are plain app settings.'],
    honest: ['Key Vault certificates are deliberately out of scope; Key Vault already emits expiry events and our Key Vault Expiry Digest covers them properly.', 'Front Door and CDN managed certificates are excluded because Azure rotates and alerts on those itself.'],
    roles: [{ role: 'Reader', why: 'enumerate gateways, App Service certificates, and API Management certificates and read their public data' }],
    writes: null, alerts: 'the Teams webhook you configure', price: null,
    blog: '/blog/azure-tls-certificate-expiry-alerts',
  },
  {
    slug: 'keyvault-digest', name: 'Key Vault Expiry Digest', group: 'Secrets & certificates',
    title: 'One digest of every expiring Key Vault secret, key, and certificate',
    tagline: 'Key Vault notifications are per vault, fire once, and ignore objects with no expiry date. This is one daily digest across every vault in the subscription.',
    description: 'Azure Key Vault expiry notifications are per vault, fire once through Event Grid, cannot escalate, and say nothing about objects with no expiry set. Key Vault Expiry Digest reports all of that daily across every vault, from inside your subscription.',
    problem: 'Each vault can notify once, through Event Grid, if someone wired it up. There is no recurring reminder, no view across vaults, and nothing at all about the secrets that never expire because nobody set a date. Those are the ones that get forgotten.',
    features: [
      'Enumerates every Key Vault in the subscription and every key, secret, and certificate in them.',
      'Reports expired objects, objects expiring within your thresholds, and enabled objects with no expiry set.',
      'Re-reports daily until each object is renewed or given an expiry, so nothing lapses quietly.',
      'Reads object metadata only, names and dates, through the management plane. It can never read secret values or key material.',
      'One Adaptive Card to Teams per day, worst first.',
    ],
    captions: ['The digest: expired, expiring, and never-expiring objects across all vaults.', 'Per-object rows with vault, type, and days remaining.', 'Scan history in your own Application Insights.'],
    honest: ['It reports; it does not rotate or set expiry dates for you.', 'Vaults the identity cannot read are listed as unreadable rather than silently skipped.'],
    roles: [{ role: 'Key Vault Reader', why: 'list vaults and read object metadata, never values' }],
    writes: null, alerts: 'the Teams webhook you configure', price: null,
    blog: '/blog/key-vault-expiring-secrets-digest',
  },
  {
    slug: 'cost-breaker', name: 'Cost Breaker', group: 'Cost control',
    title: 'A hard spending cap for your Azure bill',
    tagline: 'Budgets only email you while the money burns. Cost Breaker watches month-to-date spend hourly and stops non-exempt compute when your cap is hit.',
    description: 'Azure has no hard spending limit, the most-voted request on its billing forum for over a decade. Cost Breaker runs in your subscription, checks month-to-date cost every hour, warns at 80 percent, and at 100 percent deallocates non-exempt VMs and stops non-exempt web apps.',
    problem: 'Azure budgets are notifications, not limits. A runaway deployment or a forgotten VM keeps billing after every alert has fired. The request for a real cap has been open on the billing forum for thirteen years and is the single most-voted item there.',
    features: [
      'Hourly check of month-to-date actual cost through Cost Management, compared in your billing currency.',
      'One Teams warning per month at the warning threshold (default 80 percent).',
      'At the cap, deallocates running VMs and stops running web apps not tagged costbreaker-exempt=true, and posts exactly what it did.',
      'Ships in dry run: it reports what it would stop until you set DRY_RUN=false.',
      'Least-privilege custom role that can list, deallocate, and stop, and nothing else; it cannot create, delete, or reconfigure.',
    ],
    captions: ['The trip card: spend against cap, what was deallocated, what was stopped, what was exempt.', 'The hourly guard and the warning card at 80 percent.', 'Exemption by tag, so production stays up while the sandbox is cut.'],
    honest: ['It stops compute: VMs and web apps, the big runaway line items. Storage, egress, and committed costs keep billing. It is a circuit breaker for compute, not a guaranteed hard cap, and the listing says so.', 'Cost Management data lags real usage by hours, so the cap trips after the fact, not at the exact dollar.'],
    roles: [
      { role: 'Cost Management Reader', why: 'read month-to-date spend' },
      { role: 'Cost Breaker Enforcer (custom role)', why: 'list and deallocate VMs, list and stop web apps; no create, delete, or reconfigure' },
    ],
    writes: 'Deallocating VMs and stopping web apps that are not tagged exempt, only after the cap is reached and only once you have turned dry run off.',
    alerts: 'the Teams webhook you configure', price: 39,
    blog: '/blog/azure-spending-limit-hard-cap',
  },
  {
    slug: 'cost-spike-sentinel', name: 'Cost Spike Sentinel', group: 'Cost control',
    title: 'Per-team Azure cost anomaly alerts, one baseline per resource group or tag',
    tagline: 'Native anomaly detection is subscription-wide and capped at five rules. This gives every resource group or tag value its own baseline and its own alert.',
    description: 'Azure cost anomaly alerts run at subscription scope with a five-rule cap, so one team\'s spike averages away in the total. Cost Spike Sentinel checks each resource group or tag value against its own trailing baseline daily and tells the owning team.',
    problem: 'A four-times jump in one team\'s resource group is invisible in a large, steady subscription total, and you cannot stand up an anomaly rule per team because Azure caps you at five. The people who could fix a spike are the last to hear about it.',
    features: [
      'Daily per-slice cost from Cost Management, sliced by resource group or by a tag such as Team, CostCenter, or Environment.',
      'Trailing baseline per slice (mean and standard deviation) with a z-score test and a minimum dollar delta to suppress noise on tiny slices.',
      'Handles Cost Management latency explicitly: the newest days are restated by Azure, so the judged day is offset and the baseline excludes it.',
      'Short-history slices fall back to a percent-jump rule; brand-new slices are never alerted.',
      'Anomalies ranked by dollar delta and posted once per slice per day.',
    ],
    captions: ['The daily card: each spiking slice with latest cost, baseline, delta, and z-score.', 'Slicing by tag value gives each team its own detector.', 'Latency handling: the judged day and the baseline window.'],
    honest: ['Alert only. It never stops, caps, deallocates, deletes, or modifies anything; it has no write permission at all. Cost Breaker is the sibling that enforces.', 'Amounts are in your billing currency as Cost Management reports them, which lag real usage by a day or more.'],
    roles: [{ role: 'Cost Management Reader', why: 'read daily cost per slice' }, { role: 'Reader', why: 'resolve resource groups and tags' }],
    writes: null, alerts: 'the Teams webhook you configure', price: null,
    blog: '/blog/azure-anomaly-detector-retirement-cost-spikes',
  },
  {
    slug: 'orphan-cleanup', name: 'Orphan Cleanup', group: 'Cost control',
    title: 'Find and delete orphaned Azure resources that still bill every month',
    tagline: 'Unattached disks, orphaned NICs, unassociated public IPs, and empty App Service plans, found daily with an estimated monthly saving on each.',
    description: 'Azure has no sweep for resources attached to nothing. Orphan Cleanup finds unattached managed disks, orphaned network interfaces, unassociated public IPs, and empty App Service plans every day, estimates what they cost, and can remove them.',
    problem: 'A VM gets deleted and its disk, NIC, and public IP stay behind, billing quietly. Multiply by every decommission and every failed deployment and the leak is real. There is no native report and no native cleanup.',
    features: [
      'Daily inventory of unattached managed disks, orphaned NICs, unassociated Standard public IPs, and empty App Service plans.',
      'Per-item and total estimated monthly cost on the report.',
      'Resources tagged orphan-cleanup-exempt=true are never touched.',
      'Report-only and dry run by default; deletion needs both an explicit setting and the Contributor role.',
      'Posted to Teams as an Adaptive Card each morning.',
    ],
    captions: ['The daily report: each orphan with its type, resource group, and estimated monthly cost.', 'The totals: what the subscription is paying for nothing.', 'Cleanup mode, once enabled, records exactly what it removed.'],
    honest: ['Savings figures are estimates from list prices and the report says so.', 'It looks for four resource types with clear orphan semantics; it does not judge whether a running resource is still needed.'],
    roles: [{ role: 'Reader', why: 'inventory disks, NICs, public IPs, and App Service plans' }, { role: 'Contributor (only if you enable deletion)', why: 'remove confirmed orphans' }],
    writes: 'Deleting orphaned resources, only after you enable cleanup, turn dry run off, and grant Contributor; exempt-tagged resources are never touched.',
    alerts: 'the Teams webhook you configure', price: null,
    blog: '/blog/find-and-delete-orphaned-azure-resources',
  },
  {
    slug: 'jit-rbac', name: 'JIT RBAC', group: 'Access & identity',
    title: 'Just-in-time Azure role elevation with Teams approval, without Entra P2',
    tagline: 'Make me Contributor for a few hours, then take it away automatically. A flat-fee alternative to PIM for Azure resource roles.',
    description: 'JIT RBAC gives time-boxed Azure role assignments with a Teams approval card and automatic removal when the window ends, deployed as a managed application in your subscription. It covers Azure resource RBAC, the elevation people actually buy Entra P2 for.',
    problem: 'Standing Contributor and Owner assignments are how most Azure estates end up over-privileged, and the fix Microsoft sells, Privileged Identity Management, requires Entra P2 for every user. The day-to-day need is small: a few hours of a role, approved by someone, gone afterwards.',
    features: [
      'A request names the requester, role, scope, duration, and justification; an approval card lands in Teams.',
      'Approve and Deny links are HMAC-signed, expire, and require an explicit confirmation click, so a link preview or scanner can never approve.',
      'On approval the requester is resolved through Microsoft Graph and the role assignment is created at the requested ARM scope.',
      'A five-minute timer removes each assignment when its duration elapses. That removal is the guarantee.',
      'Every request, decision, and expiry is recorded in the app\'s own table storage for audit.',
    ],
    captions: ['The approval card in Teams: who, which role, which scope, how long, and why.', 'Active elevations with their expiry, and the audit trail behind them.', 'Request flow from a Teams command or an internal page to the function.'],
    honest: ['It manages Azure resource RBAC at subscription, resource group, and resource scope. It does not manage Entra directory roles such as Global Administrator; those need the PIM API and P2.', 'The requesting surface is an HTTP endpoint you wire to Teams, a Logic App, or a page; a chat bot is not included.'],
    roles: [{ role: 'User Access Administrator', why: 'create and remove role assignments at the scopes JIT manages' }, { role: 'Microsoft Graph User.Read.All', why: 'resolve requesters to real people and reject unknown principals' }],
    writes: 'Creating a role assignment on approval and removing it when the window ends. Nothing else is written.',
    alerts: 'the Teams channel behind your Workflows webhook', price: null,
    blog: '/blog/just-in-time-azure-role-elevation-without-pim',
  },
  {
    slug: 'rbac-janitor', name: 'RBAC Janitor', group: 'Access & identity',
    title: 'Clean up orphaned "Identity not found" Azure role assignments',
    tagline: 'Deleted principals leave role assignments behind that clutter IAM and count toward the 4,000 limit. Find them daily, and remove them safely if you choose.',
    description: 'When a user, group, or service principal is deleted, its Azure role assignments remain as orphaned "Identity not found" entries that count against the 4,000 per-subscription limit. RBAC Janitor finds them daily and can remove them, with fail-closed verification.',
    problem: 'Every IAM blade in a mature subscription shows a few "Identity not found" rows. They are harmless until the day a deployment fails because the subscription hit its role assignment limit and nobody can find which of the four thousand are dead. There is no native bulk cleanup.',
    features: [
      'Lists every role assignment in the subscription daily and asks Microsoft Graph which principals still exist.',
      'Reports orphaned assignments and the total against the 4,000 limit, warning as it approaches.',
      'Fail-closed: an assignment is only ever deleted when Graph positively confirmed the principal is gone. Lookup failures are reported as unverified, never deleted.',
      'Circuit breaker: if more than 20 percent of assignments look orphaned, deletion is aborted and an alert is posted, on the assumption a Graph incident is the real cause.',
      'Assignments inherited from management groups and foreign-tenant principals (Lighthouse) are reported separately and never touched.',
    ],
    captions: ['The daily card: orphaned assignments, the count against the limit, and anything unverified.', 'Per-assignment detail with the missing principal id, role, and scope.', 'Cleanup mode with the fail-closed and circuit-breaker safeguards visible.'],
    honest: ['Report-only and dry run by default. Deletion needs both an explicit setting and the User Access Administrator role.', 'It removes assignments whose principal no longer exists. It does not judge whether a live principal should still have its role.'],
    roles: [{ role: 'Reader', why: 'list role assignments' }, { role: 'Microsoft Graph Directory.Read.All', why: 'confirm whether each principal still exists' }, { role: 'User Access Administrator (only if you enable deletion)', why: 'remove confirmed orphans' }],
    writes: 'Deleting role assignments whose principal Graph positively confirmed as gone, only after you enable cleanup and turn dry run off.',
    alerts: 'the Teams webhook you configure', price: null,
    blog: '/blog/clean-up-stale-azure-rbac-assignments',
  },
  {
    slug: 'role-definition-guard', name: 'Role Definition Guard', group: 'Access & identity',
    title: 'Detect owner-equivalent and dangerous custom Azure role definitions',
    tagline: 'A weekly digest of every custom role that can grant access or carries a wildcard, with a drift diff against last week. Nothing native does this.',
    description: 'Anyone with the right permission can create a custom Azure role that is quietly owner-equivalent. Azure Policy\'s check is deprecated and Defender counts owner assignments, not role contents. Role Definition Guard reads every custom role weekly and reports the dangerous ones and what changed.',
    problem: 'A custom role named "App Deployer" that grants Microsoft.Authorization/* hands its holders the ability to give themselves anything. The built-in policy that used to catch this is deprecated, and Defender for Cloud looks at who is Owner, not at what your custom roles actually permit.',
    features: [
      'Weekly scan of every custom role definition at the configured scope through the management plane, including who changed it and when.',
      'Wildcard-aware matching with notActions subtraction, so a capability is only reported when it is effectively granted.',
      'Flags dangerous patterns: bare *, Microsoft.Authorization/*, role and deny assignment writes, elevateAccess, and service-level wildcards.',
      'An owner-equivalent verdict for roles that can actually grant or modify access, distinct from merely broad roles.',
      'Drift: new dangerous roles and roles whose dangerous permission set grew are called out against last week\'s snapshot.',
    ],
    captions: ['The weekly digest: each dangerous custom role, why it is dangerous, and whether it can grant access.', 'Drift since last run: new roles and roles that grew.', 'The permission analysis, with notActions carve-outs applied.'],
    honest: ['Read-only by design. It reports; it never edits or deletes a role definition.', '"Last changed by" is shown as a principal object id, because resolving it to a name would need a Graph permission we chose not to request to keep the identity Reader-only.'],
    roles: [{ role: 'Reader', why: 'list custom role definitions and their permissions at the scanned scope' }],
    writes: null, alerts: 'the Teams webhook you configure', price: null,
    blog: '/blog/detect-azure-custom-role-definition-changes',
  },
  {
    slug: 'identity-lifecycle', name: 'Identity Lifecycle', group: 'Access & identity',
    title: 'Set an Entra ID account to expire, and reap stale guests, without P2 licensing',
    tagline: 'The cloud equivalent of accountExpires, plus a warn, disable, delete pipeline for inactive guests. Two governance features Microsoft gates behind P2, as one flat fee.',
    description: 'Identity Lifecycle brings two Entra governance automations to any tenant: account expiry driven by employeeLeaveDateTime, with warnings at 14, 7, and 1 days and disable the day after, and a stale guest reaper with warn, disable, and optional delete stages. Runs entirely in your tenant.',
    problem: 'On-premises AD had accountExpires. Entra ID has employeeLeaveDateTime, which does nothing unless you pay for Governance. Contractors, temps, and interns keep working accounts long after they leave, and guests invited for one project stay forever.',
    features: [
      'Account expiry: set employeeLeaveDateTime on any user and the app warns at 14, 7, and 1 days, then disables the account the day after.',
      'Stale guest reaper: guests inactive for N days are reported, disabled after a grace period, and optionally deleted after a further period.',
      'Stages are tracked in the app\'s own table storage, so nothing is warned twice or skipped.',
      'Ships in dry run: reports only, no changes, until you flip DRY_RUN=false after reviewing the output.',
      'Every action posted to Teams.',
    ],
    captions: ['The expiry report: who is leaving when, and what the app will do about it.', 'Stale guests by last sign-in, with their current stage.', 'Dry-run output before anything is changed.'],
    honest: ['Disabling and deleting accounts is a real write path, which is why dry run is the default and the permissions are listed plainly below.', 'Inactivity is judged from sign-in activity, which Entra reports with some delay; the grace period exists for that reason.'],
    roles: [
      { role: 'Microsoft Graph User.Read.All and AuditLog.Read.All', why: 'read users and sign-in activity' },
      { role: 'Microsoft Graph LifeCycleInfo.Read.All', why: 'read employeeLeaveDateTime' },
      { role: 'Microsoft Graph User.EnableDisableAccount.All and User.ReadWrite.All', why: 'disable expired accounts and stale guests, and delete guests if you enable it' },
    ],
    writes: 'Disabling accounts past their leave date and disabling or deleting stale guests, only after dry run is turned off and the grace periods you set have elapsed.',
    alerts: 'the Teams webhook you configure', price: null,
    blog: '/blog/set-an-entra-id-account-to-expire',
  },
  {
    slug: 'policy-exemptions', name: 'Policy Exemption Tracker', group: 'Governance & hygiene',
    title: 'Track Azure Policy exemptions so none lapses or lingers unnoticed',
    tagline: 'Exemptions with an expiry lapse silently and break deployments. Exemptions without one become permanent gaps. One daily report covers both.',
    description: 'Azure Policy exemptions expire silently, and the moment one does the policy enforces again and deployments that relied on the waiver fail. Exemptions with no expiry never lapse and quietly become permanent. Policy Exemption Tracker reports both daily across the subscription.',
    problem: 'Someone grants an exemption to unblock a release, sets ninety days, and moves on. Ninety days later a deployment fails and nobody connects the two. The exemptions with no date are worse: they are security waivers with no owner and no end.',
    features: [
      'Daily list of every Azure Policy exemption in the subscription.',
      'Reports expired exemptions, where the policy is now enforcing again and recent deployments may fail.',
      'Reports exemptions expiring within your thresholds, so they can be renewed or allowed to lapse on purpose.',
      'Reports exemptions with no expiry set, the permanent ones.',
      'Read-only; posted to Teams as an Adaptive Card.',
    ],
    captions: ['The daily card: expired, expiring, and permanent exemptions.', 'Per-exemption detail with the policy, scope, and date.', 'Threshold settings.'],
    honest: ['It reports lifecycle; it does not renew or remove exemptions, and it does not evaluate whether an exemption was justified.'],
    roles: [{ role: 'Reader', why: 'list policy exemptions and their expiry dates' }],
    writes: null, alerts: 'the Teams webhook you configure', price: null,
    blog: '/blog/azure-policy-exemptions-tracking',
  },
  {
    slug: 'storage-hygiene', name: 'Storage Hygiene Scorecard', group: 'Governance & hygiene',
    title: 'Grade every Azure storage account on access security, daily',
    tagline: 'Anonymous blob access, shared-key auth, TLS floor, SAS policy, network default action: each account scored, the gaps named, the systemic ones ranked.',
    description: 'Storage misconfiguration is a top source of Azure data exposure and there is no recurring report on how accounts are configured. Storage Hygiene Scorecard grades every storage account daily against weighted checks and posts the estate view to Teams.',
    problem: 'One storage account with anonymous blob access left on is a breach. Across dozens of accounts created by different teams over years, nobody has the recurring view. The portal shows one account at a time and the checks are spread across five blades.',
    features: [
      'Daily score for every account: anonymous blob access disabled, HTTPS only, minimum TLS 1.2, shared-key auth disabled, SAS expiration policy set, network default action Deny, public network access restricted.',
      'Reports the estate average, the accounts below your threshold with their specific gaps, and the most common systemic gaps.',
      'Reads management-plane configuration only, never account keys or data.',
      'Skips its own runtime storage account (tagged storage-hygiene-self=true), which cannot score 100 because the Functions runtime requires shared-key auth.',
      'Posted to Teams as an Adaptive Card.',
    ],
    captions: ['The scorecard: estate average and the accounts below threshold.', 'Per-account gaps, named exactly.', 'Systemic gaps ranked across the estate.'],
    honest: ['It grades configuration; it does not change it. Remediation is yours, and some checks (shared-key off, network Deny) can break workloads if applied blindly.'],
    roles: [{ role: 'Reader', why: 'read storage account configuration' }],
    writes: null, alerts: 'the Teams webhook you configure', price: null,
    blog: '/blog/azure-storage-account-hygiene',
  },
  {
    slug: 'auto-tagger', name: 'Auto Tagger', group: 'Governance & hygiene',
    title: 'CreatedBy and CreatedOn tags on every Azure resource, automatically',
    tagline: 'Who created this resource and when. Azure never answers that; Auto Tagger stamps it on every new resource within fifteen minutes.',
    description: 'Azure does not record who created a resource in a place you can query. Auto Tagger polls the Activity Log every fifteen minutes and stamps CreatedBy and CreatedOn tags on newly created resources, from a managed application in your subscription.',
    problem: 'Every cost review and every cleanup starts with the same question, who made this, and Azure cannot answer it once the Activity Log ages out. The feedback request has hundreds of votes and a decade of silence.',
    features: [
      'Polls the subscription Activity Log every fifteen minutes for resource creation events.',
      'Stamps CreatedBy and CreatedOn tags on each newly created resource.',
      'Never overwrites a tag that already exists.',
      'Read access plus tag write only; it cannot modify anything but tags.',
    ],
    captions: ['Tags as they appear on a resource: the creator and the timestamp.', 'Overview of tagged resources over time.', 'The polling function in your own Application Insights.'],
    honest: ['Resources created before installation stay untagged unless you backfill them yourself; the Activity Log only reaches back ninety days.', 'A resource created by a pipeline is tagged with the pipeline\'s identity, which is the truthful answer.'],
    roles: [{ role: 'Reader', why: 'read the Activity Log and resource metadata' }, { role: 'Tag Contributor', why: 'write the two tags, nothing else' }],
    writes: 'Writing CreatedBy and CreatedOn tags on newly created resources. It writes no other tag and never overwrites an existing one.',
    alerts: 'no channel; this app writes tags rather than sending alerts', price: null,
    blog: '/blog/who-created-an-azure-resource-createdby-tag',
  },
  {
    slug: 'dangling-dns', name: 'Dangling DNS Sentinel', group: 'Coverage & risk',
    title: 'Find dangling DNS records in Azure before someone takes over your subdomain',
    tagline: 'A CNAME pointing at a deleted Azure resource is a subdomain takeover waiting to happen. Microsoft ships a one-off script; this watches every day.',
    description: 'A CNAME that still points at a deleted web app, storage account, Front Door, or Traffic Manager profile lets anyone who claims that Azure name inherit your subdomain. Dangling DNS Sentinel scans every Azure DNS zone daily from inside your subscription and reports takeover-able records to Teams.',
    problem: 'Microsoft documents the risk and provides Get-DanglingDnsRecords, a PowerShell script you run once. Resources are deleted every week. The gap between those two facts is where subdomain takeovers happen.',
    features: [
      'Enumerates every CNAME across all Azure DNS zones in the subscription daily.',
      'Resolves each Azure-service target from inside your tenant and classifies it: dangling, review, unchecked, or ok.',
      'Review covers Front Door, CDN, and Traffic Manager endpoints that resolve through wildcard DNS but whose backing resource cannot be confirmed as yours.',
      'Records that fail to resolve this run are reported as unchecked, never counted as healthy.',
      'Healthy records are not re-flagged daily, to avoid alert fatigue.',
    ],
    captions: ['The daily report: dangling and review records with the target that no longer resolves.', 'Zone-by-zone coverage.', 'Classification detail for one record.'],
    honest: ['Read-only. It never changes DNS; removing or re-pointing a record is yours to do.', 'It covers Azure DNS zones. Zones hosted elsewhere are out of its view.'],
    roles: [{ role: 'Reader', why: 'list DNS zones and records' }],
    writes: null, alerts: 'the Teams Workflows webhook you configure', price: null,
    blog: '/blog/find-dangling-dns-records-azure',
  },
  {
    slug: 'backup-auditor', name: 'Backup Auditor', group: 'Coverage & risk',
    title: 'Prove every Azure VM, database, and file share is backed up',
    tagline: 'Azure never alerts that a resource is in no backup policy. Backup Auditor cross-references the inventory against every vault daily and reports the gaps.',
    description: 'Azure has no native alert for a VM, SQL database, or file share that is in no backup policy, so coverage drifts as resources are created. Backup Auditor compares the resource inventory against every Recovery Services vault daily and posts the gaps to Teams, the artifact auditors ask for.',
    problem: 'Show me proof that everything is backed up. Every auditor asks it, and the honest answer in most subscriptions is a shrug, because the new VM from last month was never added to a policy and nothing said so.',
    features: [
      'Daily inventory of backupable resources (VMs, SQL databases, Azure Files shares) through Resource Graph.',
      'Reads protected items across every Recovery Services vault in the subscription.',
      'Classifies each resource as unprotected, failing (last backup not healthy), or stale (newest recovery point older than your threshold).',
      'Posts a coverage percentage and the itemised gaps to Teams.',
      'Read-only; it never changes backup configuration.',
    ],
    captions: ['The coverage card: percentage protected, and the unprotected, failing, and stale lists.', 'Per-resource status with the vault and last recovery point.', 'Threshold and schedule settings.'],
    honest: ['Azure SQL databases use built-in point-in-time restore rather than vaults, so they are excluded from the vault check rather than reported as unprotected.', 'Azure Files shares are matched through their parent storage account\'s protection.'],
    roles: [{ role: 'Reader', why: 'inventory resources' }, { role: 'Backup Reader', why: 'read protected items and job health in every vault' }],
    writes: null, alerts: 'the Teams webhook you configure', price: null,
    blog: '/blog/find-azure-vms-without-backup',
  },
  {
    slug: 'alert-blindspot', name: 'Alert Blindspot', group: 'Coverage & risk',
    title: 'Find the Azure resources you have zero alerts on',
    tagline: 'A weekly coverage scorecard of every production-grade resource with no metric or log alert pointed at it. The ones you discover during an outage.',
    description: 'There is no native view of which resources have no alert rules. Alert Blindspot runs Resource Graph queries weekly from inside your subscription and reports every monitorable resource that no alert actually evaluates, with a coverage percentage and per-type breakdown.',
    problem: 'The resource you find out is unmonitored is the one that just went down. Alert rules accumulate over years, scoped in ways that do not cover what people assume they cover, and nobody has the list of what is genuinely watched.',
    features: [
      'Weekly inventory of monitorable, production-grade resource types (SQL, storage, App Service, Cosmos DB, Key Vault, Redis, Service Bus, Application Gateway, Front Door and CDN, VMs), overridable per tenant.',
      'Union of every metric alert and scheduled-query alert scope, compared against the inventory.',
      'Conservative coverage model: an alert counts only when Azure Monitor genuinely evaluates that resource. It errs toward uncovered, never toward a false covered.',
      'Multi-resource metric alerts are credited only for matching type and region; broad log alerts are surfaced as a note, never as coverage.',
      'Coverage percentage and a per-type breakdown, worst first, posted to Teams.',
    ],
    captions: ['The weekly scorecard: overall coverage and the worst-covered types.', 'The blind resources by type.', 'How each alert scope was credited.'],
    honest: ['Report-only; it never creates or edits an alert rule, so there is no dry run to flip.', 'Activity-log alerts are a different alert type and are not counted as metric or health coverage.'],
    roles: [{ role: 'Reader', why: 'run Resource Graph queries over resources and alert rules' }],
    writes: null, alerts: 'the Teams webhook you configure', price: null,
    blog: '/blog/find-azure-resources-with-no-alerts',
  },
];

export const azureBySlug = Object.fromEntries(azureAppDetails.map((a) => [a.slug, a]));
